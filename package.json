{
  "name": "learn-platform-root",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "node backend/seed-admin.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
